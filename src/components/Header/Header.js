import { h } from "preact";
import { Layout } from "preact-mdl";

const Header = () => (
  <Layout.Header>
    <Layout.HeaderRow>
      <Layout.Title>NLP HUE</Layout.Title>
    </Layout.HeaderRow>
  </Layout.Header>
);
export default Header;
